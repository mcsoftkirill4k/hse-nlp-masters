# Week 6 — PEFT (prompt tuning / LoRA)

Что сделал: soft prompts руками, то же через `peft`, свой `LoRALayer` + тесты, скелет файнтюна под python-код.

Запуск:
```bash
cd week_06_peft_homework
jupyter notebook week_06_peft_homework.ipynb
```

Нужен GPU и место под LLaMA-7B (4-bit). Без CUDA дальше первых ячеек не уехать.
